import { PlaceholderScreen } from '@/app/PlaceholderScreen'

export function SettingsScreen() {
  return (
    <PlaceholderScreen
      title="More"
      description="Base currency, locale, and export will land in later epics."
    />
  )
}
